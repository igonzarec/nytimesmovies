name: ingenio_challenge
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.18.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  dio: ^4.0.6
  flutter_bloc: ^8.1.1
  cupertino_icons: ^1.0.2
  freezed_annotation: ^2.1.0
  url_launcher: ^6.1.6
  intl: ^0.17.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  build_runner: ^2.2.1
  freezed:
  test:

flutter:
  uses-material-design: true

  assets:
    - assets/